/** Maximum serialized size accepted for signed email-auth capability tokens. */
export const EMAIL_AUTH_TOKEN_MAX_LENGTH = 8192;

/** Maximum encoded claims payload accepted inside an email-auth capability token. */
export const EMAIL_AUTH_RESOURCE_ID_MAX_LENGTH = 4096;
